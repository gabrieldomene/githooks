j = [1, 
     2, 
     3, 
     4]

from random import random, choice


def very_important_function(
    template: str, 
    file: str, 
    engine: str, header: bool = True, 
    debug: bool = False
):
    """Applies `variables` to the `template` and writes to `file`."""
    return "something"


if __name__ == "__main__":
    very_important_function("teste", 
                            "engine_1", "False", 
                            False, True)
